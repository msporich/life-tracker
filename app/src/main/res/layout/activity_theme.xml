<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ThemeActivity"
    android:background="?attr/bgColor">

    <Switch
        android:id="@+id/changeThemeSwitch"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:textColor="?attr/textColor"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/modeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dark Mode"
        android:textColor="?attr/textColor"
        android:layout_marginLeft="10dp"
        app:layout_constraintBottom_toBottomOf="@+id/changeThemeSwitch"
        app:layout_constraintStart_toStartOf="@+id/changeThemeSwitch"
        app:layout_constraintTop_toTopOf="@+id/changeThemeSwitch" />


</androidx.constraintlayout.widget.ConstraintLayout>